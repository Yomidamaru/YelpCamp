#!/usr/bin/env node
var args = process.argv.splice(2)
  , colors = require('colors')

if(args[0] == 'install' || args[0] == 'update') {
  require('./mpn').run(args)
} else {
  console.log('error: you should do: mpn install'.red.bold.underline)
}
